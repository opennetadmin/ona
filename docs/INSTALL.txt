these are just rambling thoughts at this point

requirements:
mysql
php
mod-php

have to create a db called "ona"

add a user called ona-sys with a password of "onar0x"

give taht user full read/write type privs.. doenst need admin (create drop etc)


==== DCM CONFIGURATION ====

Set web host to "localhost:80" or something.
Set dcm interface path (default: "/dcm.pl").
