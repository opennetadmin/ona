these are just rambling thoughts at this point

requirements:
apache2-server
mysql-server
php4
	php4-gmp
	php4-mysql
-------------
This is the original way to install

cd /var/www
sudo svn co http://svn.opennetadmin.com/repos/ona
sudo touch /opt/ona/log/ona.log
sudo chown www-data /opt/ona/log/ona.log

---------------
This is the new way I'd like to look into

cd /opt
tar -zxvf opennetadmin-1.0.tar.gz

adduser ona
addgroup ona
ona:x:1001:1001:OpenNetAdmin,,,:/opt/ona:/bin/false
chown -R ona:ona /opt/ona

cd /opt/ona

add a symlink in your web server root that points ona -> /opt/ona/www
this may also require some apache config stuff that makes it right

export PATH=$PATH:/opt/ona/bin

add a symlink of /tftpboot/ona -> /opt/ona/<something>


mysqladmin -u root create ona
mysql -u root ona < ona.sql
mysql -u root ona -e "GRANT ALL ON ona.* TO ona_sys@localhost IDENTIFIED BY 'youshouldchangethis'"



FIXME: at this point the apache DocumentRoot should not have a trailing / as it screws up the baseURL variable.


I recommend using something like eaccelerator.. so far it seems to help things out
